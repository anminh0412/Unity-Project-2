using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WaterDieEvent : MonoBehaviour
{
    private void OnTriggerEnter(Collider other)
    {
       /* if (other.gameObject.CompareTag("Player"))
        {
            //other.gameObject.GetComponent<CharacterHealthController>().CharacterTakeDame(9999f, false, null);
        }
        if (other.gameObject.CompareTag("Enemy"))
        {
            //other.gameObject.GetComponent<TakeDameFromPlayer>().TakeDame(9999f, true);
        }*/
    }
}
