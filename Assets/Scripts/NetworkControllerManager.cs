using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Photon.Pun;
using KinematicCharacterController.Examples;

public class NetworkControllerManager : MonoBehaviour
{
    /*
    public GameObject playerMomentController;
    public GameObject playerAttackController;

    PhotonView view;

    private void Start()
    {
        view = GetComponent<PhotonView>();
    }
    private void Update()
    {
        if (view.IsMine)
        {
            playerMomentController.GetComponent<ExamplePlayer>().isMine = true;
            playerAttackController.GetComponent<CharacterAttackController>().isMine = true;
        }
        else
        {
            //playerMomentController.GetComponent<ExamplePlayer>().isMine = false;
            //playerAttackController.GetComponent<CharacterAttackController>().isMine = false;
        }
    }*/
}
